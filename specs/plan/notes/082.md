# WI-082: Close resolved domain questions and archive housekeeping

## Tasks

### 1. Close distribution Q-12
In `specs/domains/distribution/questions.md`, update Q-12 status:
- Status: resolved
- Resolution: CLAUDE.md updated in WI-073 (cycle 013) to reflect restructure sub-package with phase modules
- Resolved in: cycle 013

### 2. Update domains/index.md cross-cutting note
The note at bottom of `specs/domains/index.md` says:
> **CLAUDE.md stale after restructure decomposition**: CLAUDE.md still references `restructure.py` as a single file. The module is now a sub-package. See distribution/Q-12.

This was fixed in WI-073. Remove or update this note to reflect resolution.

### 3. Archive orphaned cycle-013 incremental reviews
Move these files from `specs/archive/incremental/` to `specs/archive/cycles/013/incremental/`:
- `072-cycle13.md`
- `073-cycle13.md`
- `074-cycle13.md`
- `075-cycle13.md`

Create `specs/archive/cycles/013/incremental/` if it doesn't exist.

### 4. Verify archive/incremental/ is clean
After moving, `specs/archive/incremental/` should be empty (cycle 015 reviews were already archived).
