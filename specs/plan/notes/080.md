# WI-080: Update ARCHITECTURE.md for current codebase

## Scope

The File Reference section (lines 889-959) is the primary target. The high-level architecture diagram (lines 10-50) is mostly still accurate but references should be verified.

## File tree updates needed

Current tree root is `cyberbrain/` — should be `src/cyberbrain/` (or show full repo structure).

### extractors/ section (lines 893-905)
Add missing files:
- `state.py` — centralized path constants
- `quality_gate.py` — LLM-as-judge quality gate
- `evaluate.py` — evaluation framework (dev tool)
- `search_index.py` — already listed, verify description

Update `extract_beats.py` description: was "Entry point — re-exports all modules", now "CLI entry point; orchestrates extraction pipeline" (re-exports removed in WI-075).

### hooks/ section (lines 907-909)
Add:
- `session-end-reindex.sh` — SessionEnd index refresh hook (detached)
- `hooks.json` — Plugin hook registration

### prompts/ section (lines 911-918)
- Count: 19 → 23
- Add missing prompt families: synthesize-{system,user}.md, quality-gate-system.md, evaluate-system.md

### mcp/tools/ section (lines 924-933)
- `restructure.py` → `restructure/` sub-package with pipeline.py, collect.py, cluster.py, cache.py, audit.py, decide.py, generate.py, execute.py, format.py, utils.py

### tests/ section (line 938)
- `~17,600 LOC, 16 modules` → `~26,300 LOC, 24 modules, 1300 tests`

### Top-level files (lines 935-946)
Add:
- `pyproject.toml` — package metadata, dependencies, tool config
- `VERSION` — version file (1.1.0)
- `.claude-plugin/plugin.json` — Claude Code plugin manifest
- `.mcp.json` — MCP server launch config
- `mcp/start.sh` — MCP server launcher script
- `CHANGELOG.md`, `QUICKSTART.md`

Remove:
- `build.sh` (deleted in WI-078)

### Installed locations section (lines 948-959)
This section describes the pre-plugin `~/.claude/cyberbrain/` install layout with `venv/`, symlinked hooks, etc. Update to describe:
- Plugin mode: `${CLAUDE_PLUGIN_ROOT}` with uv-managed deps
- Config stays at `~/.claude/cyberbrain/config.json`
- Logs, search index, caches at `~/.claude/cyberbrain/`
- No more venv/ or symlinked hooks

## Reference
The authoritative current state is in `specs/plan/architecture.md` and the architect survey from this session. Use those as sources of truth.
