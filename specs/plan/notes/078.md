# WI-078: Delete stale build artifacts

## Files to delete

1. `build.sh` — deprecated build script that packages pre-src-layout structure. Has deprecation notice from WI-037.
2. `dist/` — contains stale `cyberbrain_mcp-0.1.0-py3-none-any.whl` and `.gitignore`. No longer matches current version or layout.
3. `src/cyberbrain/extractors/requirements.txt` — orphaned, incomplete dependency subset.
4. `src/cyberbrain/mcp/requirements.txt` — orphaned, incomplete dependency subset.

## Verification

Before deleting, grep the codebase for references to these files:
- `build.sh` — check no script or doc references it (CLAUDE.md, README, install.sh)
- `requirements.txt` — check no install path references `pip install -r` against these files
- `dist/` — no references expected

## Notes

- `pyproject.toml` is the authoritative dependency source. These requirements.txt files predate the src-layout migration and specify an incomplete subset.
- The `dist/` directory's `.gitignore` can be replaced by a root `.gitignore` entry if `dist/` needs to be ignored for future builds. Check if root `.gitignore` already covers it.
