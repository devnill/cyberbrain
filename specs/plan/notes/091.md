# WI-091: Mark architecture doc tensions T5/T6/T7 resolved

## Changes

In specs/plan/architecture.md, update three tension sections following the pattern used for T2.

### T5 (line 454)
```markdown
### T5: Hook vs MCP Extraction Path Divergence ~~[RESOLVED]~~

{existing description}

> _Resolved (2026-03-23): `run_extraction()` shared function created in `extract_beats.py`. Both `main()` (CLI/hook path) and `cb_extract` (MCP path) delegate to it, eliminating orchestration duplication. See WI-088._
```

### T6 (line 458)
```markdown
### T6: Search Backend Initialization ~~[RESOLVED]~~

{existing description}

> _Resolved (2026-03-23): `_invalidate_search_backend()` added to `shared.py`. `cb_configure` calls it when `search_backend` or `embedding_model` changes, resetting the module-level cache. See WI-087._
```

### T7 (line 462)
```markdown
### T7: Relation Vocabulary Divergence ~~[RESOLVED]~~

`vault.py` defines `VALID_PREDICATES = {"related", "references", "causes", "caused-by", "supersedes", "implements", "contradicts"}`. The extraction prompt instructs the LLM to use the same vocabulary. Unknown predicates are normalized to "related".

> _Resolved (2026-03-23): Migrated from SKOS vocabulary to merged 7-predicate set based on research at `steering/research/relation-vocabulary.md`. See WI-086._
```
