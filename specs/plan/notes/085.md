# WI-085: Fix ARCHITECTURE.md prompt count

## Problem
ARCHITECTURE.md says "23 LLM prompt templates" but actual count on disk may differ.

## Fix
Run `ls src/cyberbrain/prompts/*.md | wc -l` to get the actual count. Update the count in ARCHITECTURE.md line ~936. If the listing below the count is incomplete, add missing files.
