# WI-079: Update README for current architecture

## Stale sections

### Flow diagram (lines 22-46)
- `~/.claude/hooks/pre-compact-extract.sh` → `hooks/pre-compact-extract.sh` (plugin root)
- `~/.claude/cyberbrain/extractors/extract_beats.py` → `src/cyberbrain/extractors/extract_beats.py`
- Add note that plugin system handles hook registration

### Requirements (lines 54-59)
- `Python 3.8+` → `Python 3.11+`
- Add `uv` as a requirement (dependency management)

### Installation (lines 63-76)
- Lead with plugin installation (primary path):
  ```
  claude plugin marketplace add devnill/cyberbrain
  claude plugin install cyberbrain@devnill-cyberbrain
  ```
- Move `install.sh` to a "Manual Installation" subsection for Claude Desktop users
- Remove references to venv creation at `~/.claude/cyberbrain/venv/`

### Uninstallation (lines 80-86)
- Add plugin uninstall path alongside `uninstall.sh`

## Sections to leave unchanged
- Config table (lines 114-143) — already accurate and comprehensive
- Beat types, frontmatter, per-project config — all current
- "How it works" prose at lines 47-50 — still accurate

## Reference
- CLAUDE.md Distribution section has the correct plugin install commands
- `specs/plan/architecture.md` Distribution section has the correct architecture description
