# WI-084: Clean up install.sh/uninstall.sh references in README

## Problem
README references install.sh at lines 81, 105, 417, 427, 515, 523 and uninstall.sh at lines 105, 524. Neither file exists on disk.

## Fix
- Installation section: already leads with plugin install. Remove `bash install.sh` block and replace manual install instructions with clone + uv run pattern.
- Uninstallation section: replace `bash uninstall.sh` with plugin uninstall command.
- MCP setup section: remove `install.sh` references in the Claude Desktop subsection.
- Troubleshooting: remove install.sh reference.
- File reference table: remove install.sh and uninstall.sh entries.

## Manual install replacement
For users who clone the repo directly:
```bash
git clone https://github.com/devnill/cyberbrain.git
cd cyberbrain
uv sync
```
Then configure vault path via cb_configure.
