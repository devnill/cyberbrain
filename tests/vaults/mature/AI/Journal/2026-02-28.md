# 2026-02-28

## Session Notes

- Investigated auth token refresh bug — confirmed the race condition theory. Token refresh requests overlap when multiple tabs are open. Added mutex lock around refresh calls.
- Reviewed rate limiter workaround with the team. Consensus: keep the temporary bypass for the demo but schedule removal for next sprint.
- Started config module refactor. Extracted environment-specific settings into separate files. Tests still passing after initial restructure.
